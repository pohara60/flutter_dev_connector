// Server IP address and port
//! localhost does not work with Android emulator which uses 10.0.0.2
//! To support both Web and Android emulator use the (local) IP address of the server
const BASE_URL = 'http://192.168.1.159:5000'; //100
